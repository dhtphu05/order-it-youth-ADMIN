import { ReactNode } from 'react';

/**
 * Layout for POS (Point of Sale) section
 * Handles Quick Order and other public POS features
 */
export default function POSLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
